import 'package:flutter/material.dart';

class UberCar extends StatelessWidget {
  const UberCar({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      'lib/images/car.png'
    );
  }
}